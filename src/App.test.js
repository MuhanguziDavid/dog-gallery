import { render, screen } from '@testing-library/react';
import App from './App';

test('renders a dogs gallery', () => {
  throw new Error();
})

test('renders a dog breed gallery', () => {
  throw new Error();
})

test('clicking dog breed in the list opens a modal', () => {
  throw new Error();
})

test('modal once opened contains a picture of the dog and its name', () => {
  throw new Error();
})

test('clicking a photo in the gallery opens a gallery of pictures of that breed on a new page', () => {
  throw new Error();
})
